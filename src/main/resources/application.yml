spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/software_architecture
    username: postgres
    password: Vetv3434
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /docs
  api-docs:
    path: /api-docs

logging:
  level:
    org.hibernate.SQL: DEBUG