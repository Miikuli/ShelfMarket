server:
  port: 8082

spring:
  application:
    name: shelf-booking-service
  datasource:
    url: jdbc:postgresql://localhost:5432/software_architecture
    username: postgres
    password: Vetv3434
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
